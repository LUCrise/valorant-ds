<% if @agents.any? %>
  <ul>
    <% @agents.each do |agent| %>
      <li class = "name">
        <div>
          <p><%= link_to agent.name, agent %>///<%= link_to '削除', agent, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %></p>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>